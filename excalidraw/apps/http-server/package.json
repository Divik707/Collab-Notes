{
  "name": "http-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm run build && pnpm run start",
    "build": "tsc -b",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace: *",
    "@repo/schemas": "workspace: *",
    "@repo/database": "workspace: *",
    "@repo/common2febe": "workspace: *"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "bcrypt": "^6.0.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
